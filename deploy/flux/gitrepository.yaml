apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: atlas
  namespace: flux-system
spec:
  interval: 1m
  url: https://gitlab.internal/platform/atlas   # replace with internal GitLab repo URL
  ref:
    branch: main
  secretRef:
    name: atlas-repo-auth
